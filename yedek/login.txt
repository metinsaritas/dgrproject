 @if (Route::has('login'))
                <div class="top-right links">
                    @auth
                        <a href="as{{ url('/salla') }}">Home</a>
                    @else
                        <a href="{{ route('login') }}">Login</a>
                        <a href="{{ route('register') }}">Register</a>
                    @endauth
                </div>
            @endif